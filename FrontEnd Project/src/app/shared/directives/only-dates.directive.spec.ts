import { OnlyDatesDirective } from './only-dates.directive';

describe('OnlyDatesDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDatesDirective();
    expect(directive).toBeTruthy();
  });
});
